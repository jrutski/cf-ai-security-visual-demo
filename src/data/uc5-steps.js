/**
 * UC5 — Secure Self-Hosted AI Agents (Moltworker Pattern)
 * Deploy and run self-hosted AI agents on Cloudflare's Developer Platform with
 * security at every layer — from the agent runtime to tool access to external API calls.
 *
 * Based on the Moltworker architecture:
 *   https://blog.cloudflare.com/moltworker-self-hosted-ai-agent/
 *   https://github.com/cloudflare/moltworker
 *
 * Key products: Access, Sandbox SDK / Containers, AI Gateway, Browser Rendering,
 * R2 (persistent storage), Workers (entrypoint), Secrets Store
 *
 * External API/tool calls from the agent sandbox are proxied through the entrypoint
 * Worker, which can inject credentials, validate requests, and audit all outbound traffic.
 */

export const uc5 = {
  id: 'uc5',
  title: 'Secure Self-Hosted AI Agents',
  subtitle: 'Run AI agents on Cloudflare with defense-in-depth — from runtime isolation to tool access',

  nodes: [
    // Left column — Agent configuration / admin
    {
      id: 'admin-user',
      label: 'Admin / Operator',
      sublabel: 'Configures and manages agent',
      icon: '\u{1F464}',
      type: 'user',
      column: 'left',
      description: 'The administrator or operator who configures, deploys, and manages the self-hosted AI agent. Accesses the admin UI and API endpoints protected by Cloudflare Access.',
    },
    {
      id: 'chat-client',
      label: 'Chat Client',
      sublabel: 'Slack, Discord, web UI',
      icon: '\u{1F4AC}',
      type: 'user',
      column: 'left',
      description: 'The messaging interface (Slack, Discord, web chat) through which users interact with the AI agent. Messages are routed to the agent\'s entrypoint Worker.',
    },
    // Center column — Cloudflare platform
    {
      id: 'cf-access',
      label: 'Cloudflare Access',
      sublabel: 'Zero Trust authentication',
      icon: '\u{1F512}',
      type: 'cloudflare',
      column: 'center',
      product: 'Cloudflare Access',
      description: 'Protects the agent\'s APIs and admin UI with identity-based policies. Access handles authentication and includes a JWT token with every request for origin-side validation. Service tokens enable machine-to-machine auth for automated integrations.',
      docsUrl: 'https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/',
    },
    {
      id: 'worker-entrypoint',
      label: 'Worker Entrypoint',
      sublabel: 'API router & proxy',
      icon: '\u{2699}',
      type: 'cloudflare',
      column: 'center',
      product: 'Cloudflare Workers',
      description: 'The entrypoint Worker acts as an API router and proxy between external services and the agent\'s sandbox container. Validates JWT tokens from Access, manages the sandbox lifecycle, and proxies requests between the agent and external services. All outbound API calls from the agent are routed through this Worker for credential injection and auditing.',
      docsUrl: 'https://developers.cloudflare.com/workers/',
    },
    {
      id: 'sandbox',
      label: 'Sandbox / Container',
      sublabel: 'Isolated agent runtime',
      icon: '\u{1F4E6}',
      type: 'cloudflare',
      column: 'center',
      product: 'Cloudflare Sandbox SDK',
      description: 'The AI agent runtime runs in an isolated Sandbox container. Each sandbox has its own container with filesystem, process, and network isolation. The Sandbox SDK provides APIs for command execution, file management, and service exposure — all from the entrypoint Worker.',
      docsUrl: 'https://developers.cloudflare.com/sandbox/',
    },
    {
      id: 'ai-gateway',
      label: 'AI Gateway',
      sublabel: 'LLM proxy + observability',
      icon: '\u{1F9E0}',
      type: 'cloudflare',
      column: 'center',
      product: 'Cloudflare AI Gateway',
      description: 'All LLM API calls from the agent are routed through AI Gateway via the env.AI binding configured in wrangler.jsonc. The Worker passes a gateway option (gateway: { id: "my-gateway" }) when calling env.AI.run() — no base URL changes needed. BYOK (Secrets Store) manages provider keys centrally so the agent never sees plaintext API keys. Alternatively, Unified Billing provides centralized credits without individual provider keys. Logging, rate limiting, caching, and DLP control costs and provide observability.',
      docsUrl: 'https://developers.cloudflare.com/ai-gateway/',
    },
    {
      id: 'secrets-store',
      label: 'Secrets Store',
      sublabel: 'Centralized key management',
      icon: '\u{1F511}',
      type: 'cloudflare',
      column: 'center',
      product: 'Cloudflare Secrets Store',
      description: 'Stores all AI provider API keys, external service credentials, and integration tokens. Keys are referenced by name in AI Gateway and Worker configurations — never passed in plaintext request headers.',
      docsUrl: 'https://developers.cloudflare.com/secrets-store/',
    },
    // Right column — External services
    {
      id: 'llm-provider',
      label: 'LLM Provider',
      sublabel: 'OpenAI, Anthropic, etc.',
      icon: '\u{1F916}',
      type: 'ai-service',
      column: 'right',
      description: 'External LLM providers (OpenAI, Anthropic, Google AI, Workers AI) accessed through AI Gateway. The agent\'s LLM calls are proxied, logged, rate-limited, and cached.',
    },
    {
      id: 'browser-rendering',
      label: 'Browser Rendering',
      sublabel: 'Headless Chromium',
      icon: '\u{1F310}',
      type: 'cloudflare',
      column: 'right',
      product: 'Cloudflare Browser Rendering',
      description: 'Headless Chromium instances for web browsing tasks — navigating, form-filling, screenshots, and scraping. The agent accesses Browser Rendering via a CDP proxy through the entrypoint Worker, not directly from the sandbox.',
      docsUrl: 'https://developers.cloudflare.com/browser-rendering/',
    },
    {
      id: 'r2-storage',
      label: 'R2 Storage',
      sublabel: 'Persistent agent memory',
      icon: '\u{1F4BE}',
      type: 'cloudflare',
      column: 'right',
      product: 'Cloudflare R2',
      description: 'Persistent object storage for agent memory, conversations, session data, and generated assets. R2 buckets can be mounted as filesystem paths inside the sandbox container using sandbox.mountBucket(), surviving container restarts.',
      docsUrl: 'https://developers.cloudflare.com/sandbox/tutorials/persistent-storage/',
    },
    {
      id: 'external-apis',
      label: 'External APIs / Tools',
      sublabel: 'fetch() to services',
      icon: '\u{1F4E1}',
      type: 'resource',
      column: 'right',
      description: 'External APIs, SaaS services, and tools that the agent needs to access (calendars, financial APIs, social media, etc.). All outbound fetch() calls from the agent are proxied through the entrypoint Worker for credential injection, request validation, and audit logging.',
    },
  ],

  edges: [
    { id: 'e-admin-access', from: 'admin-user', to: 'cf-access', label: 'OIDC/SAML', direction: 'ltr' },
    { id: 'e-chat-access', from: 'chat-client', to: 'cf-access', label: '', direction: 'ltr' },
    { id: 'e-access-worker', from: 'cf-access', to: 'worker-entrypoint', label: 'JWT', direction: 'ltr' },
    { id: 'e-worker-sandbox', from: 'worker-entrypoint', to: 'sandbox', label: 'SDK API', direction: 'ltr' },
    { id: 'e-worker-secrets', from: 'worker-entrypoint', to: 'secrets-store', label: '', direction: 'ltr' },
    { id: 'e-sandbox-aig', from: 'sandbox', to: 'ai-gateway', label: 'LLM calls', direction: 'ltr' },
    { id: 'e-aig-llm', from: 'ai-gateway', to: 'llm-provider', label: '', direction: 'ltr' },
    { id: 'e-worker-browser', from: 'worker-entrypoint', to: 'browser-rendering', label: 'CDP', direction: 'ltr' },
    { id: 'e-sandbox-r2', from: 'sandbox', to: 'r2-storage', label: '', direction: 'ltr' },
    { id: 'e-worker-apis', from: 'worker-entrypoint', to: 'external-apis', label: 'Proxy', direction: 'ltr' },
    { id: 'e-llm-sandbox-resp', from: 'llm-provider', to: 'sandbox', label: '', direction: 'rtl' },
    { id: 'e-sandbox-chat-resp', from: 'sandbox', to: 'chat-client', label: 'Reply', direction: 'rtl' },
  ],

  steps: [
    {
      title: 'Admin/user authenticates via Access',
      product: 'Cloudflare Access',
      description: 'The admin or user authenticates through Cloudflare Access using their identity provider (OIDC/SAML). Access protects both the agent\'s admin UI and API endpoints. A JWT token is included with every request for origin-side validation.',
      why: 'Zero Trust Access ensures only authorized users can interact with or configure the AI agent. JWT validation at the Worker level provides defense-in-depth — even if the endpoint URL leaks, unauthenticated requests are rejected.',
      activeNodes: ['admin-user', 'chat-client', 'cf-access'],
      activeEdges: ['e-admin-access', 'e-chat-access'],
      docsUrl: 'https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/',
      owasp: ['ASI03 Identity & Privilege Abuse', 'ASI10 Rogue Agents'],
    },
    {
      title: 'Worker entrypoint routes request',
      product: 'Cloudflare Workers',
      description: 'The entrypoint Worker validates the Access JWT, routes the request (admin UI, chat message, or API call), and manages the sandbox lifecycle. It acts as the secure bridge between the authenticated user and the isolated agent runtime.',
      why: 'The Worker entrypoint is the single point of control — all interactions with the agent pass through it. JWT validation, request routing, and sandbox management are centralized here.',
      activeNodes: ['cf-access', 'worker-entrypoint'],
      activeEdges: ['e-access-worker'],
      docsUrl: 'https://developers.cloudflare.com/workers/',
    },
    {
      title: 'Agent runs in isolated Sandbox',
      product: 'Cloudflare Sandbox SDK',
      description: 'The AI agent runtime executes inside an isolated Sandbox container. Each sandbox has its own container with filesystem, process, and network isolation. The Sandbox SDK provides the Worker with APIs to execute commands, manage files, and control the agent lifecycle.',
      why: 'Container-level isolation ensures the agent cannot access other tenants\' data or escape its runtime boundary. Per-user sandbox instances prevent cross-contamination between different agents or sessions.',
      activeNodes: ['worker-entrypoint', 'sandbox'],
      activeEdges: ['e-worker-sandbox'],
      docsUrl: 'https://developers.cloudflare.com/sandbox/',
      owasp: ['ASI05 Unexpected Code Execution (RCE)', 'LLM06:2025 Excessive Agency'],
    },
    {
      title: 'Secrets Store provides credentials',
      product: 'Cloudflare Secrets Store',
      description: 'All API keys, provider credentials, and integration tokens are stored in Cloudflare Secrets Store. The Worker references secrets by name — they are never exposed in code, environment variables visible to the agent, or request headers.',
      why: 'Centralized secret management eliminates plaintext credentials. The agent sandbox never sees raw API keys — credential injection happens at the Worker level, preventing accidental exposure through agent logs or LLM context.',
      activeNodes: ['worker-entrypoint', 'secrets-store'],
      activeEdges: ['e-worker-secrets'],
      docsUrl: 'https://developers.cloudflare.com/secrets-store/',
      owasp: ['LLM02:2025 Sensitive Information Disclosure'],
    },
    {
      title: 'AI Gateway proxies LLM calls',
      product: 'Cloudflare AI Gateway',
      description: 'The entrypoint Worker calls env.AI.run() with a gateway: { id: "my-gateway" } option, routing all LLM requests through AI Gateway automatically. The env.AI binding is configured in wrangler.jsonc. env.AI.gateway("my-gateway") provides additional methods: getUrl() for SDK integration, patchLog() for feedback, getLog() for analytics. BYOK manages provider keys via Secrets Store. Rate limiting, caching, DLP, and logging are applied.',
      why: 'AI Gateway integrates via the env.AI Worker binding — a single wrangler config and gateway ID option. This means you can switch providers, enable DLP, or add rate limiting by updating the gateway dashboard without redeploying the agent.',
      activeNodes: ['sandbox', 'ai-gateway', 'llm-provider'],
      activeEdges: ['e-sandbox-aig', 'e-aig-llm'],
      docsUrl: 'https://developers.cloudflare.com/ai-gateway/',
      owasp: ['LLM10:2025 Unbounded Consumption', 'ASI02 Tool Misuse & Exploitation'],
    },
    {
      title: 'Browser Rendering for web tasks',
      product: 'Cloudflare Browser Rendering',
      description: 'The agent uses headless Chromium instances via Browser Rendering for web tasks — navigating pages, filling forms, taking screenshots, and extracting content. A CDP (Chrome DevTools Protocol) proxy in the Worker routes browser commands from the sandbox to Browser Rendering.',
      why: 'Browser Rendering provides managed headless browsers without running Chromium inside the container. The CDP proxy pattern keeps browser access controlled through the Worker — the sandbox never directly accesses Browser Rendering.',
      activeNodes: ['worker-entrypoint', 'browser-rendering'],
      activeEdges: ['e-worker-browser'],
      docsUrl: 'https://developers.cloudflare.com/browser-rendering/',
    },
    {
      title: 'R2 persists agent memory',
      product: 'Cloudflare R2',
      description: 'R2 buckets are mounted as filesystem paths inside the sandbox using sandbox.mountBucket(). Session memory, conversation history, and generated assets persist across container restarts. The agent reads/writes files normally — R2 provides the durable backing store.',
      why: 'Containers are ephemeral — data is lost on deletion. R2 bucket mounting provides seamless persistence: the agent interacts with local file paths while data is durably stored in R2 with zero egress fees.',
      activeNodes: ['sandbox', 'r2-storage'],
      activeEdges: ['e-sandbox-r2'],
      docsUrl: 'https://developers.cloudflare.com/sandbox/tutorials/persistent-storage/',
    },
    {
      title: 'External API calls proxied securely',
      product: 'Cloudflare Workers',
      description: 'All outbound fetch() calls from the agent to external APIs (calendars, financial services, SaaS tools) are proxied through the entrypoint Worker. The Worker validates the request, injects credentials from Secrets Store, logs the API call, and forwards the request. The agent sandbox never holds external service credentials directly.',
      why: 'Proxied API calls provide credential injection (the agent never sees raw API keys), request validation (the Worker can block disallowed endpoints), and audit logging (every external call is recorded). This is the secure pattern for agent-to-external-service communication.',
      activeNodes: ['worker-entrypoint', 'external-apis'],
      activeEdges: ['e-worker-apis'],
      docsUrl: 'https://developers.cloudflare.com/sandbox/guides/proxy-requests/',
      owasp: ['ASI02 Tool Misuse & Exploitation', 'LLM02:2025 Sensitive Information Disclosure'],
    },
    {
      title: 'LLM response returned to agent',
      product: 'Cloudflare AI Gateway',
      description: 'The LLM response is returned through AI Gateway (with optional DLP scanning on responses) back to the agent in the sandbox. The agent processes the response and formulates its reply to the user.',
      why: 'Response-path DLP via AI Gateway ensures the LLM doesn\'t leak sensitive data back to the agent. The complete LLM interaction is logged for observability and compliance.',
      activeNodes: ['llm-provider', 'sandbox', 'ai-gateway'],
      activeEdges: ['e-llm-sandbox-resp'],
      docsUrl: 'https://developers.cloudflare.com/ai-gateway/features/dlp/',
      owasp: ['LLM02:2025 Sensitive Information Disclosure'],
    },
    {
      title: 'Agent reply delivered to user',
      product: 'Cloudflare Workers + Sandbox',
      description: 'The agent\'s reply is delivered back to the user through the chat client (Slack, Discord, web UI). The complete round-trip was secured: authentication via Access, isolated execution in Sandbox, LLM calls through AI Gateway, credentials via Secrets Store, and all external API calls proxied and audited.',
      why: 'End-to-end security: every layer from user authentication to agent execution to external API access is controlled and audited through Cloudflare\'s platform. No dedicated hardware needed — the entire agent infrastructure runs on Cloudflare\'s global network.',
      activeNodes: ['sandbox', 'chat-client'],
      activeEdges: ['e-sandbox-chat-resp'],
    },
  ],
};
